@host = http://localhost:8061/api

### User Login
# This endpoint is used for user authentication. 
# The user provides their username and password, and if the credentials are valid, 
# a token is returned in the Set-Cookie header. 
# This token should be used for subsequent authenticated requests.

POST {{host}}/login HTTP/1.1
Content-Type: application/json
Accept: application/json

{
  "username": "user01",
  "password": "password"
}




